# `schemes` package

Validated configuration objects for every CLI entry point.

The single source of truth is [`PipelineConfig`](argument.md) in
`schemes.argument`. The package also re-exports a `Args` back-compat alias.

::: schemes
