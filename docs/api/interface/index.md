# `interface` package

Gradio (and Streamlit) console interfaces, tabs, and the realtime detection UI.

The `detection-gui` console script launches this via `interface.gui:main`.

::: interface
::: interface.gui
