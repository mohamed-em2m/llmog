# `auto_annotation` package

Batch relabel binary defect/no-defect YOLO annotations into multi-class defect
labels using a vision-language model. This is the split-up version of the
former single-file `auto_annotiontion.py`.

::: auto_annotation
