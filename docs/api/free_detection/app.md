# Gradio UI

The `detection-gui` entry point (`free_detection.gui:main`) and the Gradio app
itself (`free_detection.app`). The app is built by `interface.app_builder.build_app`.

::: free_detection.gui
::: free_detection.app